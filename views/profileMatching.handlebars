<p> Welcome, {{userData.[0].user.name}} </p>

<p>{{userData.message}}</p>

The services you are sharing:
{{#each userData as |userData|}}
  <p> {{userData.streamingService.stream_name}}  </p>
{{/each}}

<form class="form start-sharing">
    <button type="submit" class="btn btn-primary">Sharing Services</button>
</form>



{{userData.[2].message}}

{{!-- ////////////// --}}

<form class="form select-platform">
    <div class="form-group">
        <label for="select-platform">Select Platform</label>
        <input type="text" class="form-control" id="platform">
    </div>
    <button type="submit" class="btn btn-primary">Confirm</button>
</form>


<script src="/js/profileMatching.js"></script>