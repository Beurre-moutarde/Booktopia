<div class="container mt-5">
    <div class="row">
        <div class="col-md-6">
            <div class="card p-3">

                <div class="d-flex align-items-center mb-3">
                    <h3 class="me-3">Welcome, {{data.[0].[0].name}}</h3>

                    <form class="start-sharing">
                        <button type="submit" class="btn btn-primary btn-sm">ADD SUBSCRIPTION</button>
                    </form>

                    <h2>{{data.[1].message}}</h2>

                </div>
                <p>{{sharingData.message}}</p>
                <h3>The services you are sharing:</h3>
                <div>

                    {{#if data.[1].length}}
                    <ul>
                        {{#each data.[1]}}

                        <li>{{this.streamingService.stream_name}}</li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p>You are not sharing any services at the moment.</p>
                    {{/if}}
                </div>
                <h3>You are now using:</h3>
                <div>

                    {{#if data.[2].length}}
                    <ul>
                        {{#each data.[2]}}

                        <li>{{this.streamingService.stream_name}}</li>
                        {{/each}}
                    </ul>
                    {{else}}
                    <p>You are not using any services at the moment.</p>
                    {{/if}}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card p-3">
                <div class="d-flex align-items-center mb-3">

                    <div class="no-wrap">
                        <h3 class="me-3">SUBSCRIPTION</h3>
                    </div>

                    <select id="right-service-dropdown" class="form-select">
                        <option value="Netflix">Netflix</option>
                        <option value="AmazonPrime">Amazon Prime Video</option>
                        <option value="DisneyPlus">Disney+</option>
                        <option value="HBO">HBO</option>
                        <option value="ParamountPlus">Paramount+</option>
                    </select>

                    <form class="select-platform">
                        <button type="submit" class="btn btn-primary ms-3">Confirm</button>
                    </form>

                </div>
                <div class="mb-3 text-center">
                    <h3><label for="application-login" class="form-label">The login credentials are:</label></h3>
                    <input id="application-login" type="text" placeholder="Application Login" class="form-control">
                </div>
                <div class="mb-3 text-center">

                    <input id="application-password" type="text" placeholder="Application Password"
                        class="form-control">
                </div>
            </div>
        </div>
    </div>
</div>
<footer class="bg-light text-center py-3">
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h4>Contact Us</h4>
                <ul class="list-unstyled">
                    <li>Address: 123 Main St, Melbourne, Australia</li>
                    <li>Phone: 111-111-1111</li>
                    <li>Email: info@passwordsharingapp.com</li>
                </ul>
            </div>
            <div class="col-md-6">
                <h4>Disclaimer</h4>
                <p>All passwords shared on this app are done so at the user's own risk. We are not responsible for any
                    misuse or illegal activities that may occur as a result of password sharing.</p>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12">
                <p>&copy; 2023 Password Sharing App. All Rights Reserved. | Country: Australia</p>
            </div>
        </div>
    </div>
</footer>
<script src="/js/profileMatching.js"></script>